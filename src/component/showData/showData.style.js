import { TableCell, Typography } from "@mui/material";
import styled from "styled-components";

export const EmptyData = styled(Typography)(() => ({
  marginTop: "50px",
  color: "#fff",
}));
export const TableCellText = styled(TableCell)(() => ({
  color: "#fff !important",
}));
